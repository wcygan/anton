# context/

Forward-looking architectural decisions and their context.

- `adrs/` — Architecture Decision Records (immutable; supersede instead of edit)

Authored via the `adr` skill (`.claude/skills/adr/`). The current ADR index is also injected into every Claude Code session by `.claude/hooks/inject_adr_index.py`.
