---
status: In-progress
opened: 2026-05-05
closed: null
affects: compute
intent: concrete-need
related-adrs: [0017, 0021, 0022]
related-postmortems: [2026-05-05-k8s-1-k8s-3-dual-silent-reboot]
supersedes-plan: 0009
review-by: 2026-06-05
---

# 0013 — localize cluster-wide silent-reboot mechanism (successor to 0009)

> Re-cut of the silent-reboot investigation after the 2026-05-05 cross-node event falsified plan 0009's k8s-2-only premise. Closes the postmortem's 12 action items and decides between localized fix, layered-defenses + extended uptime, or accept-and-mitigate.

## Goal

Following the 2026-05-05 sequential silent-reboots of k8s-3 (19:07:13Z, verified via live `/proc/uptime` + boot ID `e55639d4…`) and k8s-1 (~19:33Z, postmortem estimate; not independently verifiable since the 20:29:07Z graceful reboot overwrote on-node evidence) — a **~26-minute gap**, with k8s-2 holding throughout — localize the actual silent-reboot mechanism. Now known to be cluster-wide capable, with **sequential cross-node cascade (~26 min)** as the leading expression shape, and with BIOS version not predictive of susceptibility. Drive the 2026-05-05 postmortem's 12 action items to closure. End in one of three terminal states: **(a)** a localized root cause is identified and a committed fix lands, OR **(b)** the layered defenses (kernel-side trace surface, per-node reboot detection, Unknown-pod alerting, HA on critical-path Deployments) are in place and 14 consecutive days of zero unexplained reboots are observed across all three nodes, OR **(c)** mechanism remains unlocalized after the watch window AND mitigations are sufficient — explicitly accept and live with it, document the residual risk.

## Acceptance criteria

- [ ] Vector kernel sink demonstrably captures kernel-side trace through the *next* silent-reboot event on whichever node(s) it hits — closes the AI-1 forensic gap that bit us on 2026-05-05 (sink torn down at `4037deec`, restored at `35533bdb`)
- [ ] Cluster self-detects any-node unexpected reboot AND `Unknown`-phase pod accumulation via PrometheusRules firing through to Alertmanager — closes the AI-2 + AI-3 detection blindness exposed when `K8s2UnexpectedReboot` was the only reboot rule and detection came from an off-cluster bastion loop
- [ ] Physical power topology (PDU port / outlet / breaker / UPS group per MS-01) is **audited and recorded** as a static reference. The verified 26-minute gap between k8s-3 (19:07:13Z) and k8s-1 (~19:33Z) materially weakens the shared-power-coincidence hypothesis (a power event would have hit both nearly simultaneously, not 26 min apart), so this is no longer a "must-be-resolved-to-close" gate — but the topology itself is worth on file for any future event whose timing does fit a shared-power pattern
- [ ] Critical-path single-replica Deployments (`network/cloudflare-tunnel`, `network/envoy-gateway`, `tailscale/operator`) are HA *or* explicitly accepted as 1-replica with documented rationale; if HA, anti-affinity / topology-spread is verified to behave correctly when peer pods are in `Unknown` phase (AI-4 + AI-9)
- [ ] One of: **(a)** localized root cause identified with committed fix landed; **(b)** 14 consecutive days of zero unexplained reboots across all three nodes with the new forensic surface live; **(c)** explicit accept-and-mitigate decision recorded as an ADR with the residual-risk rationale

## Tasks

### Phase 1 — Forensic surface live and self-detection in place (P0, mostly done)

- [x] AI-1: Restore Vector kernel sink — commit `35533bdb`. (Verification of through-event capture is the open-ended Phase 4 task, not this checkbox.)
- [ ] AI-1 verification: confirm `KmsgLogConfig` runtime resource present on all 3 nodes (`talosctl get kmsglogconfigs`), Vector receiver Service has endpoints, kernel records arriving in the daily sink file from k8s-1 (note: k8s-1 source is VTEP `10.100.100.1` per cilium native-routing, not LAN IP), k8s-2, k8s-3
- [x] AI-2: PrometheusRule on `kube_pod_status_phase{phase="Unknown"}` — commit `40bb66a6`
- [ ] AI-2 fire-test: synthetic Unknown pod (force-pause kubelet on a sacrificial workload, or use the existing investigation playground) → alert lands in Alertmanager → cleared
- [x] AI-3: Generalize `K8s2UnexpectedReboot` to per-node `NodeUnexpectedReboot` family — commit `40bb66a6`
- [ ] AI-3 fire-test: a controlled `talosctl reboot --mode=default` on one node → all three of the new per-node series exercised at least once → confirm the right one fires and the other two do not
- [ ] Carry-over from plan 0009 acceptance criterion 3: cluster-wide cilium-agent memory alert (`container_memory_working_set_bytes{container="cilium-agent"} > 2048Mi for 5m`) verified end-to-end through to Alertmanager. The rule lives in `prometheusrule-cilium-memory.yaml` per plan-0009 tick-4 2026-04-23, but the fire-test was never run

### Phase 2 — Cheap retrospective passes against the held node's data (P1, software-only, no physical access required)

- [ ] **Retrospective gap analysis on k8s-1 across the 19:07Z → 19:33Z window** (the 26-minute interval between k8s-3's verified reboot and k8s-1's estimated reboot). New leading hypothesis: k8s-3's failure was the *trigger* that broke k8s-1 ~26 min later. Look on k8s-1 for any sustained state change that started at 19:07–19:08Z and was still escalating at ~19:33Z: kubelet/kube-apiserver blip recovery as k8s-3 dropped out and back in, etcd churn (leader change, slow apply, fsync stalls), Cilium endpoint/identity churn, network re-convergence (BGP / VTEP / Multus storage overlay), conntrack-table pressure, memory step, IRQ storm, BPF map operation spike. Cross-check the same metrics on k8s-2 (the node that held) as a negative control — anything pathological that hit k8s-1 but not k8s-2 is the smoking gun. Both held nodes' TSDB covers the entire window
- [ ] AI-7: Investigate ghost-pod accumulation root cause on k8s-1. Why did kubelet stop reporting on those specific pods 4 d 15 h before the reboot (i.e. ~2026-05-01T05Z)? Check the Vector log sink (which *was* live in that window) and journalctl/talosctl logs from k8s-1 around 2026-05-01T04–06Z. If a partial event went unnoticed and is on the same mechanism as 2026-05-05, that earlier event's timeline could be the missing data point
- [ ] Cross-correlate plan 0007 / plan 0009 ring-buffer dmesg evidence from prior k8s-2 silent reboots against the new cross-node sequential fingerprint. Are there shared signals (network-silence cliffs, specific kernel subsystems quiet) that would have been visible on 2026-05-05 if the kernel sink had been live? Specifically: did the prior k8s-2 events show the same kind of slow-escalation-then-reset pattern that the k8s-1 gap analysis (above) is looking for?

### Phase 3 — Discriminating tests for the next event (P1, mix of cheap and physical-access-gated)

> Reordered after the 26-min sequential framing landed: the cascade-trigger hypothesis is now the cheapest, highest-signal lever. Power topology demoted to a static-record task.

- [ ] **Cascade-trigger reproduction test (highest signal, cheap-to-medium cost).** With kernel sink and per-node reboot alerts live, perform a controlled `talosctl reboot --mode=default` on k8s-3 during a maintenance window. Watch k8s-1 for the same delayed-escalation pattern Phase 2's gap analysis surfaces. Outcomes: (i) k8s-1 shows pre-event signature within ~30 min of k8s-3 dropping out → cascade hypothesis is supported and we have a reproducer, propose targeted fix on whichever subsystem is escalating; (ii) k8s-1 stays clean → 2026-05-05's cascade was not deterministically triggered by a clean k8s-3 reboot, look elsewhere (the silent-reboot mode of failure on k8s-3 may have produced different on-network behavior than a graceful reboot does)
- [ ] **k8s-2 turbo-lockout reboot discriminator (telemetry-ensemble gate; MSR best-effort).** Following the 2026-05-06 02:30-04:30 CDT throttle episode, k8s-2 entered a sustained sub-turbo state — max core frequency capped at 2.76 GHz at first capture, slowly recovering to 3.78 GHz over hours, while k8s-1/k8s-3 sit at 2.9 GHz avg (PM-QoS-stuck active). Run a controlled `kubectl drain` + `talosctl reboot --mode=default` on k8s-2 once the existing telemetry ensemble — `node_cpu_scaling_frequency_hertz` (max + per-core), `node_rapl_package_joules_total` (package watts), `node_hwmon_temp_celsius` (coretemp), `node_cpu_package_throttles_total` — has captured at least one full stuck-state window with k8s-1/k8s-3 negative-control values over the same window. The MSR `0x64F` collector at `14640fb5` remains deployed but is best-effort; the 2026-05-06 investigation found stock Talos kernel ships `# CONFIG_X86_MSR is not set` (intentional upstream posture, declined in `siderolabs/extensions#620`), Image Factory has no kernel-CONFIG knob, and the custom-kernel path is too expensive while this plan is in flight — `.claude/agent-memory/talos-operator/project_msr_kernel_config_absent.md` for the full investigation. Outcomes: (A) max core freq returns to ≥4.5 GHz post-reboot → sticky software/firmware lockout cleared; mechanism named from the ensemble (RAPL Watt step without coretemp rise → PL1/PL2; coretemp crests TCC pre-throttle → thermal; freq cap holds with neither watts nor temp budging → PROCHOT or turbo-ratio cap), propose targeted fix on the named mechanism; (B) max core freq stays ≤3 GHz → hardware-side (VRM degraded, stuck thermal sensor, EC asserting BD_PROCHOT), escalates plan 0015 Phase 1 BIOS profile to near-term and indicates physical inspection at next visit. Pre/post evidence captured in [`../notes/k8s-2-instability/evidence-2026-05-06-k8s-2-turbo-lockout.md`](../notes/k8s-2-instability/evidence-2026-05-06-k8s-2-turbo-lockout.md); etcd quorum stays at 3/3 throughout because k8s-2 control-plane membership is preserved by graceful reboot.
- [ ] Off-cluster reboot probes for k8s-1 and k8s-3 in the bastion `loop` style, distinct from the now-done in-cluster `NodeUnexpectedReboot` rule. Belt-and-suspenders against cluster-wide failure modes that could impact the cluster's own alerting
- [ ] Controlled data-plane mesh stress test with kernel sink live: scale a workload that exercises VXLAN-storage-overlay traffic across all three nodes, looking for the pre-reset signature directly. Tests cluster-wide candidate 1b (Multus / VXLAN overlay)
- [ ] Controlled Cilium / eBPF heavy code-path test with kernel sink live: scale a workload that exercises a known-heavy BPF path (service-mesh / encryption / large endpoint churn). Tests cluster-wide candidate 1a (Cilium / eBPF)
- [ ] Carry-over from plan 0009 Phase 5: i226-V NIC firmware survey across all 3 nodes. Read current firmware version, check vendor for stability-relevant releases. Was promoted in plan 0009 tick-2 2026-04-24 to "first-class because the ~30 s network-silence signature is an ethernet-path fingerprint" — that signature is part of a cross-node event, which keeps the ethernet-path angle in play
- [ ] **Power topology audit (demoted; static reference only).** Write down exactly which outlet / PDU port / breaker / UPS each MS-01 is on. Capture as `context/notes/k8s-2-instability/evidence-2026-05-XX-power-topology.md`. The 26-min gap between k8s-3 and k8s-1 weakens the shared-power-coincidence hypothesis materially; the audit is no longer near-term plan-critical, but the topology is worth on file for any future event whose timing does fit a shared-power pattern
- [ ] (Deprioritized but not discarded) DIMM swap on whichever node next presents during a physical-access window. k8s-2 was the prior target under the now-falsified single-unit hypothesis; under the cluster-wide hypothesis space, no specific node is privileged. Cheap to do during the next physical visit, but no longer plan-critical

### Phase 4 — Wait-for-next-event capture (open-ended, runs continuously)

- [ ] Maintain the bastion-side off-cluster watch loop indefinitely (the 30-min cron loop that caught 2026-05-05 was running for an unrelated reason). Promote it from "per-investigation prop" to a permanent fixture
- [ ] When the next event fires: capture kernel-side trace from the Vector sink (now live), capture per-node reboot alert from the in-cluster path (now live), capture Unknown-pod alert if/as ghosts re-accumulate (now live). Write up as `context/incidents/<date>-...` + `context/postmortems/<date>-...` + `context/notes/k8s-2-instability/evidence-<date>-....md` per the established convention
- [ ] If a kernel trace *is* captured: localize the failure mechanism to the named subsystem in the trace; propose targeted fix; close criterion (a)
- [ ] If kernel sink stays silent through the next event: the failure happens below kernel logging horizon (hard CPU wedge / microcode-induced reset / power event). Promote the power-topology + NIC-firmware paths to top priority; reconsider whether DIMM swap on the next-presenting node moves up

### Phase 5 — HA on critical-path Deployments (P1, software-only)

- [ ] AI-4 — `network/cloudflare-tunnel`: bump to ≥2 replicas with topology-spread / anti-affinity. Verify Cloudflare side accepts multiple connector replicas to the same tunnel (it does — Cloudflare load-balances across them)
- [ ] AI-4 — `network/envoy-gateway`: bump to ≥2 replicas with topology-spread / anti-affinity. Verify the Envoy Gateway controller chart supports HA without leader-election conflicts
- [ ] AI-4 — `tailscale/operator`: bump to ≥2 replicas if the operator chart supports HA. If it does not, document the constraint and accept the 1-replica posture explicitly
- [ ] AI-9: Investigate `envoy-external` topology-spread interaction with `Unknown`-phase peer pods. The 2026-05-05 incident showed both running replicas ended up on k8s-1 because the prior peers (now Unknown ghosts) on other nodes were satisfying anti-affinity from the API's perspective. Likely needs `whenUnsatisfiable: DoNotSchedule` plus careful label-exclusion of Unknown-phase pods; may not be expressible — investigate and document the result either way

### Phase 6 — Operator runbooks and chaos drill (P2/P3)

- [ ] AI-5: Document the direct-kubeconfig fallback (`talosctl kubeconfig` → sed server URL to a Tailscale IP → `--insecure-skip-tls-verify` due to cert SAN mismatch) in the `anton-remote-access` skill. The 2026-05-05 incident used this workaround for ~30 min; it should not be ad-hoc
- [ ] AI-11: Author `unwedge-after-node-reboot.md` runbook documenting the batched force-delete + cordon + graceful-reboot recovery sequence. This is the second time it has been needed (plan 0007 day-1 + 2026-05-05); promote from pattern recognition to documented procedure
- [ ] AI-8: Add a quarterly chaos-engineering drill — rotate one node's pods (cordon + drain) and verify all critical paths remain serving. Would have surfaced the latent ghost-pod accumulation days earlier on 2026-05-05
- [ ] AI-10: External uptime probe (Cloudflare Workers / GitHub Action / Hetzner cron) for `<public-site-A>` and `<public-site-B>`. Closes the gap where the platform learns about user-facing outages independently of cluster health
- [ ] AI-12: Investigate `seaweedfs-volume-1` PVC pinning to k8s-1 — should this volume be free to attach anywhere, or is the pin intentional? Stale `VolumeAttachment` deletion was needed to unblock recovery on 2026-05-05

### Phase 7 — Decision gate and close-out

- [ ] At T+14d post-Phase-1 (~2026-05-19) OR at next unexplained reboot, whichever comes first: re-evaluate. Choose terminal state (a) localized fix, (b) extended clean uptime, or (c) accept-and-mitigate
- [ ] If (c): author an ADR via the `adr` skill capturing the accept-and-mitigate decision, the residual-risk rationale, and the mitigations that are load-bearing (kernel sink, alerts, HA, runbooks). Link the ADR back here
- [ ] **Cross-close plan 0007.** Its Phase 5 teardown task was specifically the kernel sink that we just restored — that task is moot. Close 0007 as Done, citing this plan as the new home for the kernel-sink ownership, or as Abandoned if the framing no longer fits
- [ ] Invoke `planner close 0013 done "<closing note>"` (or `abandoned` if criterion (c) applies but no ADR was warranted)
- [ ] If a durable architectural decision emerged that's worth preserving (e.g. "permanent kernel-sink ownership", "external uptime probe is non-optional infrastructure", "1-replica is unacceptable for path X"), hand off to the `adr` skill

## Log

- 2026-05-05: Plan opened from plan 0009 abandonment after the 2026-05-05T19:33Z dual silent-reboot of k8s-1 + k8s-3 (k8s-2 held). The k8s-2-only framing 0009 was scoped against is falsified; this plan picks up under the cluster-wide / dual-node-capable hypothesis space. Hypothesis re-cut and discriminating-test backlog drawn from `context/notes/k8s-2-instability/evidence-2026-05-05-dual-silent-reboot.md` and the 12 prioritized action items in `context/postmortems/2026-05-05-k8s-1-k8s-3-dual-silent-reboot.md`. Three of the postmortem's P0 items (AI-1 kernel sink restore, AI-2 Unknown-pod alert, AI-3 per-node reboot alert) are already committed (`35533bdb` + `40bb66a6`); their fire-test verifications are open Phase 1 tasks. Plan 0007 Phase 5 cross-close is tracked as a Phase 7 task here rather than re-opening 0007.
- 2026-05-05 (correction): The 2026-05-05 event was originally framed as a "dual silent-reboot within ~28 s" (k8s-3 19:33:54Z → k8s-1 19:34:22Z). Verified truth is **sequential, ~26 min apart**: k8s-3 booted at **19:07:13Z** (live `/proc/uptime` + boot ID `e55639d4…`), k8s-1 silent-rebooted at **~19:33Z** (postmortem estimate, not independently verifiable since the 20:29:07Z graceful reboot overwrote on-node evidence). k8s-2 held throughout. Plan updated: Goal reframed as "sequential cross-node cascade (~26 min)"; acceptance criterion 3 demoted from "must-be-resolved gate" to "audit-and-record" (the 26-min gap weakens the shared-power-coincidence hypothesis); Phase 2's retrospective-Prometheus-pass task replaced with a 19:07Z → 19:33Z gap analysis on k8s-1 vs k8s-2 negative control, under the new "k8s-3's failure was the trigger that broke k8s-1" hypothesis; Phase 3 reordered to put a **cascade-trigger reproduction test** (controlled k8s-3 reboot, watch k8s-1) on top, with the power topology audit demoted to a static-record task lower in the list. Postmortem (`context/postmortems/2026-05-05-...`), incident (`context/incidents/2026-05-05-...`), and `context/notes/k8s-2-instability/README.md` still need parallel correction (will pause for user confirmation before proceeding).
- 2026-05-06 (thermal canary correction): The `idle-mitigations` DaemonSet from commit `de801143` successfully blocked the deep-idle failure surface but set `/dev/cpu_dma_latency` to `0 us`, which proved stricter than the comment claimed: on k8s-2, cpuidle counters showed POLL-only behavior, not C0/C1. k8s-2 then developed sustained package/core throttle counters starting at `2026-05-06T12:27Z`; k8s-1/k8s-3 stayed at zero. Commit `f7b8cafc` changed k8s-2 only to `1 us`, allowing `C1_ACPI` / `MWAIT 0x0` while keeping `C2_ACPI` and `C3_ACPI` / `MWAIT 0x60` blocked. Ten-minute watch after rollout: package/core throttle rates fell to zero, CPU package temperature dropped into roughly the 57-69 C band, `nvme_nvme1` Sensor 1 started trending down, and k8s-2 stayed Ready. Incident: `context/incidents/2026-05-06-k8s-2-thermal-throttle.md`; postmortem: `context/postmortems/2026-05-06-k8s-2-thermal-throttle.md`. Follow-up: add throttle counters to the precursor observability set and physically inspect k8s-2 cooling/fan/tach behavior.
- 2026-05-06 (telemetry rollout + second k8s-2 throttle episode): Closes the prior entry's "add throttle counters to the precursor observability set" follow-up. Three new telemetry surfaces landed in `observability/`:
  - **Intel RAPL** via node-exporter `--collector.rapl` (commits `14f979da` → `a0ac62d3`). Required `runAsUser:0` on the node-exporter container — Talos kernel rejects non-root reads of `energy_uj` regardless of `CAP_DAC_READ_SEARCH` (PLATYPUS mitigation, CVE-2020-8694). 3 series live (`node_rapl_package_joules_total`, one per node); DRAM domain (`intel-rapl:1`) is absent on MS-01 firmware so `node_rapl_dram_joules_total` will stay at 0 series until BIOS exposes it.
  - **smartctl_exporter** (chart `prometheus-smartctl-exporter` 0.16.0, commit `14f979da`). 9 series for per-drive wear / errors / thermal-mgmt transitions.
  - **nvme-power-collector** (custom privileged DaemonSet, commit `14f979da`). Polls `nvme get-feature -f 0x02` every 30s, writes `node_nvme_power_state{device}` via node-exporter's textfile collector. NVMe spec quirk surfaced and fixed (`a0ac62d3`): the Crucial CT500P3SSD8 returns Feature 02h with the Workload Hint bits (7:5) set even at PS=0, raw value 0x40 — must mask `& 0x1F` to extract the actual PS index.
  - 7 dashboard panels added to "Hardware Health — plan 0009" row (IDs 807-813): per-node package watts, cluster total, 7-day kWh, watts-vs-temp twin-axis, NVMe live NPSS, NVMe wear, NVMe thermal-mgmt transitions.
  - Two findings the new telemetry caught immediately:
    1. **Plan-0013 idle-mitigations APST-disable does not hold on the WD_BLACK SN7100 drives.** `node_nvme_power_state` shows `nvme0` at PS4 and `nvme1` at PS3 on k8s-1 even though the DaemonSet writes `nvme_core.default_ps_max_latency_us=0`. The Crucial CT500P3SSD8 (`nvme2`) sits at PS0 as expected. Either the SN7100 firmware ignores the host APST-disable, or it uses a non-APST low-power mechanism not covered by the iter #15 mitigation. Reintroduces the silent-reboot risk specifically on the SN7100s — the very drives iter #20/#28 implicated.
    2. **Second k8s-2 throttle episode**, ~02:30-04:30 CDT today (`2026-05-06T07:30-09:30Z`). Counter trajectory from in-cluster Prom: 02:34 +213, 03:04 +3548, 03:34 +5856, 04:04 +7648, 04:34 +773, then zero. Total ~18,041 package-throttle events on k8s-2 in that window vs zero on k8s-1/k8s-3 — same hardware, same firmware, same `idle-mitigations` DaemonSet. **Throttle has stopped but freq has not recovered**: at 09:30Z post-event, k8s-2 still sits at avg 0.73 GHz / 15.5 W package, while k8s-1 and k8s-3 are at 2.91 GHz / 29.7 W and 2.93 GHz / 30.1 W respectively. k8s-2 has *more* work to do (CPU% 12.5, load1 2.0) but is doing it at quarter-speed clocks. Package temp is *lower* on k8s-2 (65 °C vs 69 °C) precisely because freq is being held down — classic active throttle-then-stuck signature. The `f7b8cafc` PM-QoS relaxation (`0us → 1us`) did NOT permanently resolve the thermal/power constraint on this specific node; it just deferred the next event by ~21 hours. Strongly suggests the issue is k8s-2-specific hardware (VRM, PSU brick, cooler contact, stuck PROCHOT) rather than the C-state mitigation tuning.
  - **Follow-ups for next session**: (a) add Grafana panels 814 + 815 — a "throttle active" state-timeline plus a "throttle why" overlay (freq + temp + watts + load1 + PL1 utilization) so the operator can read thermal-vs-power-budget at a glance; (b) decide whether to apply per-drive APST exception via sysctl or move the SN7100s to the BIOS-1.27 / iter #28 HMB-disable path explicitly (HMB=0 already attempted but APST=0 isn't sticking on these drives); (c) physical inspection of k8s-2 cooling — the throttle-then-low-freq-stuck pattern has now happened twice in 21 hours, and BIOS profile + runtime caps are insufficient. Plan 0015 Phase 1 (BIOS turbo-ratio caps, X710 PCIe Gen3 fix, Wi-Fi/CNVi disable) likely needs to land on k8s-2 before the next scheduled physical visit.
- 2026-05-06 (k8s-2 fan1 onset corrected — long-running pattern, not a 2026-05-06 onset). Three parallel subagents reviewed the timeline / queried betty's TSDB / generated remote-fix hypotheses. Agent B's data archeology against in-cluster Prom's 7-day retention and betty vmsingle (24h) found `node_hwmon_fan_rpm{instance="192.168.1.99:9100",sensor="fan1"}==0` readings on k8s-2 every day going back to 2026-04-29 (8 zeros), 04-30 (12), 05-01 (104), 05-02 (36), 05-03 (4), 05-04 (26), 05-05 (76), 05-06 ~448. k8s-1 and k8s-3 had zero zero-readings across all 8 days. The earlier "First zero observed at 02:55 CDT 2026-05-06" framing in the prior log entry and in the standalone fan note was an artifact of in-cluster Prom's scrape gaps for k8s-2 (5 fan-RPM samples vs betty's 3400 over the same 2h window — plan 0014 paid off here). Today's burst is the most severe day in the window, but the fan is a long-running marginal failure, not a new one. The hypothesis "idle-mitigations rollout caused the fan to fail" is falsified — the fan was already failing 7 days *before* idle-mitigations existed. The parallel "idle-mitigations rollout amplifies the rate via NVMe thermal coupling" hypothesis is now mostly invalidated by the iter #15 finding (next entry above): per-drive APST is at default, drives are naturally APST-sleeping, no steady-state rollout-induced heat to amplify with. Open candidates for *why today's rate stepped up* (~448 vs ~76 yesterday): workload concentration on k8s-2 active-state NVMe bursts, late-stage bearing degradation, ambient temperature, fan2 marginal wear. None directly testable remotely without provoking another bearing-stall episode. Notes corrected: `context/notes/k8s-2-instability/evidence-2026-05-06-k8s-2-fan1-intermittent-failure.md` (Summary, hard-numbers table, hypothesis chain) and `evidence-2026-05-06-k8s-2-turbo-lockout.md` (Fan1 subsection). The repair-or-replace recommendation is unchanged: physical fan replacement on k8s-2 is the durable fix; in the meantime the cluster has run with the marginal fan for 8+ days and survived.
- 2026-05-06 (iter #15 NVMe APST-disable mitigation has been silently ineffective on already-bound drives across the cluster). Discovered while pre-checking a remote experiment that would have re-enabled APST on k8s-2's SN7100s only to test the fan-rate-amplification hypothesis. The pre-check found that per-drive `/sys/class/nvme/nvme*/power/pm_qos_latency_tolerance_us` is at the kernel default of **100,000 µs** on every NVMe on every node, while the module parameter `/sys/module/nvme_core/parameters/default_ps_max_latency_us` is correctly at **0** on every node (the DaemonSet's init log even shows `[ok] nvme_core.default_ps_max_latency_us = 0 (cluster-wide)` on each pod). The module parameter only governs **new bind events** — drives bound at Talos boot inherit whatever the default was at boot time (100,000 µs), and the post-boot module-param write does not propagate to already-bound drives. The DaemonSet's else-branch fallback (per-drive write) does not fire because the module-param write succeeds. Result: APST is effectively enabled on every NVMe in the cluster despite the iter #15 mitigation; `node_nvme_power_state` confirms — k8s-2 nvme1 distribution over a 2h sample is 80 % PS3 / 20 % PS4 (drive is naturally APST-sleeping). If iter #15's premise was load-bearing for silent-reboot prevention on the SN7100s (per iter #20/#28 the suspected silent-reset path on BIOS-1.27 nodes), the fix never actually applied. The fix has two paths: (a) extend the DaemonSet to ALSO write 0 to per-drive `pm_qos_latency_tolerance_us` regardless of module-param success — survives the existing-bind window but only until next reboot when drives re-bind with the new module default of 0; (b) bake `nvme_core.default_ps_max_latency_us=0` into the Talos kernel cmdline so it's set before any drive binds — the durable fix. Recommend (b) as the primary, with (a) as a stop-gap that can land same-day. Independent question: is iter #15 actually load-bearing? The original silent-reboot suspicion on SN7100s + IOMMU rests on circumstantial evidence; if APST has been effectively enabled the whole time and the nodes have not silent-rebooted in the relevant 8+ day window, that's weak evidence the original SN7100-APST suspicion may have been wrong. The HMB-disable path (iter #28) is a separate mitigation that DOES land per-drive (the script writes to `/sys/class/nvme/nvme*/hmb`) and is unaffected by this finding. Action: open a follow-up plan-0013 task to land the kernel-cmdline fix and re-evaluate iter #15's load-bearing-ness against a clean APST-disabled cluster.
- 2026-05-06 (k8s-2 fan1 intermittent failure — leading hypothesis for the throttle episode and possibly for the parent silent-reboot pattern). New telemetry surfaced an intermittent fault on the chassis fan that may be the actual root cause of the recurring k8s-2 throttle events: `node_hwmon_fan_rpm{instance="192.168.1.99:9100",sensor="fan1"}` shows **16 zero-readings out of 145 samples** in the last 12 h on k8s-2 (≈11 % of the time at 0 RPM), against **0 of 145** on both k8s-1 and k8s-3 over the same window. Fan2 on k8s-2 is healthy (0 zeros, 2106-2854 RPM). First zero observed at 02:55 CDT 2026-05-06 *as visible from in-cluster Prometheus's 14h retention at the time of writing* — later corrected to "long-running pattern at least 8 days back"; see the immediately-following log entry. Today's first zero IS *inside the package-throttle window* (02:34 first throttle increment, peak at 03:34) — making fan stalls and throttle events co-temporal. Even when fan1 is spinning, k8s-2 runs ~20 % slower than the same fan on peer nodes (1928 RPM vs 2415-2789). Downstream airflow evidence: `nvme1` composite temp on k8s-2 is **87 °C** vs 61 / 73 °C on the same M.2 slot of k8s-1 / k8s-3 — same hardware, only differential is airflow. Reframed hypothesis for both the recurring throttle and the parent silent-reboot pattern: when fan1 stalls, VRM / chassis thermal sensors (separate from CPU coretemp) cross threshold; EC asserts BD_PROCHOT; CPU drops to base or below-base freq while CPU coretemp reads room temp because the heatsink is bonded directly to the package. A harder stall under sustained load could cross the EC's protection-reset threshold (firmware-initiated reset, zero kernel logging) — exactly the silent-reboot signature plan 0013 has been chasing. The k8s-2 reboot discriminator is downgraded: a reboot does not fix a flaky fan bearing, so Outcome B (hardware-side) is now the leading prior before the test runs. Actionable conclusion: **physical inspection / fan replacement on k8s-2** moves to the highest-priority physical-access task; the BIOS-profile work in plan 0015 Phase 1 should land in the same visit. Replacement parts are commercially available — Minisforum sells "Fan One" (CPU fan, DC12V 0.40A 4-pin Molex PicoBlade) and "Fan Two" (NVMe-side, DC5V) as separate SKUs through third-party resellers (Amazon ASIN B0FD8BVQXN, multiple eBay listings, [`linda-parts`](https://www.cdrtd.com/products/replacement-mini-pc-cpu-fan-for-minisforum-ms-01-ms-01-s1390-dc5v-0-40a-4pin-fan1-new.html)); community-favored mod is Noctua NF-A12x15 with a 5V-to-12V USB adapter (Level1Techs thread). Evidence note: [`../notes/k8s-2-instability/evidence-2026-05-06-k8s-2-turbo-lockout.md`](../notes/k8s-2-instability/evidence-2026-05-06-k8s-2-turbo-lockout.md) "Fan1 intermittent failure" section; need to determine pre-repair which physical fan maps to nct6775 fan1 (CPU vs NVMe-side) — most-likely CPU because nct6775 fan1 is typically the primary header.
- 2026-05-06 (MSR-on-Talos investigation; reboot-discriminator reframed off MSR). The 2026-05-06 commits `14640fb5` (collector) + `459628a9` (`msr` declared in `machine.kernel.modules`) landed but `/dev/cpu/*/msr` did not appear: stock Talos kernel ships `# CONFIG_X86_MSR is not set` in `siderolabs/pkgs` `kernel/build/config-amd64` line 458 (verified on `main` and `release-1.13`), so `msr.ko` does not exist and declaring the module is a no-op. Upstream resolution: `siderolabs/talos#10408` (closed 2025-02-23) and `siderolabs/extensions#620` (closed 2025-02-25) — Andrey Smirnov declined the request as "MSR access is a security violation" and pointed at sysfs alternatives (`intel_pstate`, `pmc_core`). Talos Image Factory schematic schema (`siderolabs/image-factory` `pkg/schematic/schematic.go`) has no kernel-CONFIG knob — only `extraKernelArgs`, `meta`, `systemExtensions`, `bootloader`, `secureboot`. The only escape is forking `siderolabs/pkgs` and minting a custom installer; rejected here on cost/risk grounds (custom installer rolls 3 nodes during active investigation, breaks Renovate installer-digest automation, injects a new variable into plan 0013). Phase 3 task line above reframed: the reboot discriminator now gates on the existing telemetry ensemble (`node_cpu_scaling_frequency_hertz` + `node_rapl_package_joules_total` + `node_hwmon_temp_celsius` + `node_cpu_package_throttles_total`) instead of MSR `0x64F`. The binary A/B/C decision still works without MSR; what's lost is single-read bit-level naming of the limiting reason inside Outcome A, which the ensemble can usually disambiguate (RAPL Watt step without coretemp rise → PL1/PL2; coretemp crests TCC → thermal; freq cap with flat watts and temp → PROCHOT or turbo-ratio cap). MSR collector left deployed best-effort. Tangent flagged: `siderolabs/talos#12968` (open, 2026-03-15) "Unexpected software-triggered node reboot. Reason `[0x00080800]`" — same fingerprint family (silent, software 0xCF9 write, no kernel-side trace, storage-correlated) on a different storage backend (LINSTOR/DRBD vs anton's Longhorn/Multus); worth Phase 4 watch as a possibly-shared upstream issue. Resolution memory: `.claude/agent-memory/talos-operator/project_msr_kernel_config_absent.md`.
- 2026-05-06 (close of day — iter-15 fix shipped both layers + PM-QoS 1 µs promoted cluster-wide; ~34 W cluster savings; new 7-day silent-reboot watch clock starts now). End-of-day rollup of the work that closed today:
  - **Iter-15 fix, runtime layer** (commit `5283b385`). Edited `idle-mitigations` DaemonSet Op 3 to *always* write 0 to per-drive `/sys/class/nvme/nvme*/power/pm_qos_latency_tolerance_us` after the module-param write, removing the if/else that prevented the per-drive fallback from firing when the module-param succeeded. No reboot needed; Flux reconciled, pods bounced, init re-ran. Verified post-rollout: per-drive setting = 0 on all 9 NVMes (3 × 3); `node_nvme_power_state` for the SN7100 sample drives shifted from PS3/PS4 to PS0 within minutes — first time iter-15 has been genuinely active on already-bound drives in the ~2 weeks since `de801143`.
  - **Iter-15 fix, boot layer** (commit `11e097a5`). Minted a new factory.talos.dev schematic `0e77ef9f7ee8043e144f0542531295e59b55474bbfd6a8be721f130d9be6fcf9` adding `extraKernelArgs: [nvme_core.default_ps_max_latency_us=0]` on top of the existing extensions (i915, intel-ucode, iscsi-tools, tailscale, util-linux-tools). Old schematic `445a99db4002...` retired. Required because Talos installer is UKI-based (signed image with cmdline baked in) so `install.extraKernelArgs` in machine config is rejected by talhelper with `install.extraKernelArgs and install.grubUseUKICmdline can't be used together` — the Image Factory schematic API is the supported path. Talconfig.yaml updated, per-node configs regenerated locally (gitignored). Closes the boot-window gap before the DaemonSet runs.
  - **Rolling reboot to apply the new schematic** — dispatched to talos-operator subagent. Order k8s-1 → k8s-3 → k8s-2 (k8s-2 last, hosts observability stack and is historically marginal). All three rolled successfully in ~64 minutes total (12:36-13:40 CDT), of which ~30 min was deliberate 5-min cooldowns and Flux-stable waits. etcd quorum stayed 3/3 throughout. Per-node verification: `/proc/cmdline` contains `nvme_core.default_ps_max_latency_us=0` on all 3, per-drive `pm_qos_latency_tolerance_us=0` on all 9 NVMes immediately on boot (proves cmdline took effect before any drive bound).
  - **Operator finding (saved to agent memory)**: `talosctl apply-config` for a `talosImageURL`-only diff returns "Applied configuration without a reboot" and does NOT trigger a reboot — the procedure I gave the operator was wrong. The operator pivoted to `talosctl upgrade --image=... --timeout=10m` (which writes new boot entries) followed by `talosctl reboot --wait=false` per node. Rule for next time: **same-version schematic swap = upgrade + manual reboot, two steps**. Memory: `.claude/agent-memory/talos-operator/feedback_apply_config_does_not_reflash_schematic.md`.
  - **PM-QoS 1 µs promoted cluster-wide** (commit `c63c61a0`). With iter-15 finally active and `f7b8cafc`'s 1 µs canary on k8s-2 having held for ~21 h without throttle, removed the NODE_NAME conditional in Op 1 so all three nodes hold `/dev/cpu_dma_latency = 1 µs`. C1_ACPI / MWAIT 0x0 admitted everywhere; C2/C3/C8 (the iter #6 silent-reboot surface) remain blocked. Pre/post (T-0 baseline → T+30s): k8s-1 31.8 W → 14.5 W (-17 W), 76 °C → 68 °C; k8s-3 30.3 W → 13.2 W (-17 W), 68 °C → 55 °C; k8s-2 unchanged at 13.7 W / 56 °C. Total cluster: **-34 W continuous (~12 % of package draw)**, fan duty cycle should drop accordingly which directly helps k8s-2's failing fan1. Lost: per-node canary mechanism — all three nodes now move together so any future bad change appears everywhere at once instead of being caught on one node first. Acceptable trade-off given the data and the fan situation.
  - **Open follow-ups carried into the watch window**: (1) k8s-2 fan1 physical replacement (parts ordered? — see `evidence-2026-05-06-k8s-2-fan1-intermittent-failure.md` for the three options). (2) Whether iter #15 was actually load-bearing in the first place — 8+ days of silent-reboot quiet with APST effectively enabled is weak counter-evidence; if the new properly-disabled-APST window stays quiet for 7 days, the original SN7100-APST suspicion gets weaker still. (3) The C-state asymmetry watch is gone (collapsed at promote), so any new silent reboot in the watch window has a different attribution path — must consider C1 itself as a suspect alongside the unrelated candidates. (4) The whole mitigation stack (PM-QoS 1 µs, APST disable, ASPM disable, HMB disable) deserves a single ADR when this plan closes, not four — defer ADR authoring to plan terminal state per Phase 7. (5) The third NVMe restoration on k8s-2 (3 disks present again per operator memory `project_k8s_2_3rd_nvme_restored.md`) — Longhorn topology may need adjustment. (6) Watch reset: 7-day silent-reboot clock starts effectively now (post-rolling-reboot baseline 13:40 CDT + post-PM-QoS-promote ~14:30 CDT). Both starts are valid framings; use the later one for the cleanest signal.
  - **Today's commit chain** (chronological): `5283b385` (iter-15 part a) → `11e097a5` (iter-15 part b, schematic) → `c63c61a0` (PM-QoS promote). Plus the prior in-session work: telemetry rollout, dashboard panels, evidence-note corrections, MSR investigation closeout — all cross-referenced in earlier log entries above.

## References

- **Predecessor plan:** [`0009-k8s-2-k8s-3-silent-reboot-followup.md`](0009-k8s-2-k8s-3-silent-reboot-followup.md) — closed Abandoned 2026-05-05; contains the full pre-2026-05-05 investigation history, hypothesis evolution, and the salvage-vs-carry-over notes
- **Cross-open plan:** [`0007-k8s-2-remote-diagnostic-rollout.md`](0007-k8s-2-remote-diagnostic-rollout.md) — Phase 5 teardown moot post-restore; cross-close tracked in Phase 7 here
- **Postmortem:** [`../postmortems/2026-05-05-k8s-1-k8s-3-dual-silent-reboot.md`](../postmortems/2026-05-05-k8s-1-k8s-3-dual-silent-reboot.md) — root-cause analysis, 12 action items, lessons learned
- **Incident:** [`../incidents/2026-05-05-k8s-1-k8s-3-dual-silent-reboot.md`](../incidents/2026-05-05-k8s-1-k8s-3-dual-silent-reboot.md) — operational timeline, every recovery batch
- **Driving evidence note:** [`../notes/k8s-2-instability/evidence-2026-05-05-dual-silent-reboot.md`](../notes/k8s-2-instability/evidence-2026-05-05-dual-silent-reboot.md) — per-node BIOS/boot-ID matrix, forensic posture, hypothesis re-cut, discriminating-tests table
- **Investigation README:** [`../notes/k8s-2-instability/README.md`](../notes/k8s-2-instability/README.md) — needs update to reflect the new hypothesis ranking; that update is implicit Phase 2 housekeeping
- **ADRs:** 0017 (Multus storage-network — relevant to dual-node candidate 1b), 0021 (cilium memory limit → 3584Mi — relevant to dual-node candidate 1a), 0022 (cilium cleanState retraction)
- **Commits already landed against this plan's scope:**
  - `35533bdb feat(observability): restore Vector kernel sink (revert plan-0007 teardown)` — AI-1
  - `40bb66a6 feat(observability): alert on Unknown-phase pods + per-node reboot` — AI-2 + AI-3
  - `b758fd78 docs(incidents): 2026-05-05 k8s-1 + k8s-3 dual silent reboot`
- **Cluster checks:**
  - `talosctl get kmsglogconfigs` (per node) — Phase 1 verification
  - `kubectl -n observability exec talos-log-sink-vector-0 -c rotator -- ls -la /vector-data-dir/` — Phase 1 verification
  - `kubectl get prometheusrules -A | grep -E 'unknown|reboot'` — Phase 1 verification
  - `flux get hr -A` and `flux get ks -A` — standard Flux readout
