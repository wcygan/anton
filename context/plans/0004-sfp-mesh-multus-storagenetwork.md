---
status: Done
opened: 2026-04-18
closed: 2026-04-19
affects: networking, storage
intent: concrete-need
related-adrs: [0009, 0017, 0018]
review-by: null
---

# 0004 — SFP+ mesh + Multus + Longhorn storage network

> Route Longhorn replica traffic onto the 10 Gbit SFP+ full mesh via Multus-provisioned NADs, unlocking the ADR 0009 fabric that is physically cabled but still unused.

## Goal

Longhorn replica traffic flows end-to-end over the three DAC links — not the 2.5 GbE management interface — on all three nodes. The SFP+ fabric (cabled and link-up as of 2026-04-18, per `context/hardware.md`) becomes the load-bearing storage network that ADR 0009 was written to justify. Done means an `iperf3` run between any two mesh peers clears the ≥9 Gbit/s gate and Longhorn's own replica-byte counters (node-exporter `node_network_transmit_bytes_total` on the SFP+ devices) demonstrably move during a volume write.

## Acceptance criteria

- [x] Multus thick-plugin mode deployed under `kubernetes/apps/network/multus/` via the 3-file Flux pattern; Flux `Kustomization` reports `Ready` and the Multus DaemonSet has 3/3 pods Running.
- [x] All three nodes have `/31` static addresses configured on their SFP+ interfaces via Talos `networkInterfaces`, each node reaches every mesh peer over that address, and `talosctl get links` shows `LINK STATE=true` on all 6 ports.
- [x] `iperf3 -P 4` between any two mesh peers (jumbo frames enabled, MTU 9000) achieves **≥9 Gbit/s** sustained.
- [x] A `NetworkAttachmentDefinition` named `longhorn-storage` exists in the **`storage`** namespace (where the Longhorn HelmRelease actually runs; the original plan said `longhorn-system`, which the 2026-04-19 verification corrected) and Longhorn's `defaultSettings.storageNetwork` is set to `storage/longhorn-storage`.
- [x] A test Longhorn volume under write load shows SFP+ interface `node_network_transmit_bytes_total` counters moving on the source node, and the `enp87s0` management interface counter stays flat for replica traffic.

## Tasks

### Phase 1 — Multus install (depends on ADR 0017)

- [x] Use `flux-app-author` / `add-flux-app` to scaffold `kubernetes/apps/network/multus/` (3-file pattern: `GitRepository` pointing at `k8snetworkplumbingwg/multus-cni` pinned to a thick-mode release tag, Flux `Kustomization`, and an `app/` overlay rendering `deployments/multus-daemonset-thick.yml`).
- [x] Apply kustomize patches: override namespace to `network` on the DaemonSet, ServiceAccount, ConfigMap, and ClusterRoleBinding subject; pin the image to the matching `ghcr.io/k8snetworkplumbingwg/multus-cni:<tag>-thick`. **Note:** thick-mode ships `multus-daemon-config` with `"multusConfigFile": "auto"`, which auto-discovers Cilium via `/host/etc/cni/net.d`; no explicit `clusterNetwork: cilium` patch is needed (that was a thin-mode concept).
- [x] Commit, `task reconcile`, verify the Flux `Kustomization` reports `Ready` and the Multus DaemonSet has 3/3 pods Running.
- [x] Confirm no Cilium regression — `cilium status` stays green and existing pod networking is unaffected.

### Phase 2 — Talos `/31` rolling config

The per-node `/31` assignments (from the DAC topology discovered 2026-04-18, recorded in `context/hardware.md`):

| DAC | Node A iface | Node A addr | Node B iface | Node B addr |
|---|---|---|---|---|
| DAC #1 | k8s-1 `enp2s0f0np0` | `10.100.0.0/31` | k8s-2 `enp2s0f1np1` | `10.100.0.1/31` |
| DAC #2 | k8s-1 `enp2s0f1np1` | `10.100.0.2/31` | k8s-3 `enp2s0f1np1` | `10.100.0.3/31` |
| DAC #3 | k8s-2 `enp2s0f0np0` | `10.100.0.4/31` | k8s-3 `enp2s0f0np0` | `10.100.0.5/31` |

- [x] Edit `talos/talconfig.yaml` to add the two SFP+ `networkInterfaces` entries on each node (MAC-selector + `/31` address + `mtu: 9000`). Keep `dhcp: false`, no default route (storage-only fabric).
- [x] `task talos:generate-config` and review the diff on each rendered node config.
- [x] Hand off to `talos-operator` to apply rolling, one node at a time: `task talos:apply-node IP=… MODE=auto` with etcd-quorum checks between nodes.
- [x] Verify on each node: `talosctl -n <ip> get addresses` shows the two new `/31` entries; `talosctl -n <ip> get links` shows all 6 SFP+ ports still `LINK STATE=true`.
- [x] Ping across each DAC from each side (6 directions total) to confirm bidirectional reachability.

### Phase 3 — iperf3 baseline (gate: ≥9 Gbit/s)

- [x] Deploy an `iperf3` server + client as short-lived `hostNetwork: true` pods pinned to specific nodes (or run via `talosctl`-accessible debug tooling if available).
- [x] Run `iperf3 -P 4 -t 30` in both directions across each DAC. Record results in the Log.
- [x] **Gate check**: all 6 directions ≥9 Gbit/s sustained with MTU 9000. If any link misses, stop and diagnose (MTU mismatch, flow control, pause frames, CPU IRQ affinity) before proceeding.
- [x] Tear down the iperf3 pods.

### Phase 4 — VXLAN adapter + whereabouts + NetworkAttachmentDefinition

**Scope expanded 2026-04-19** — see the Log entry of the same date. The original Phase 4 was a one-file NAD; reality is that the point-to-point `/31` mesh from Phase 2 has no shared L2 domain, which Longhorn's single-NAD storage-network model requires. The chosen adapter is a **VXLAN overlay** (VNI 100) layered on top of the three `/31` DACs, presenting a shared `10.100.1.0/24` segment that macvlan + whereabouts can consume unchanged. IPAM decision is locked to **whereabouts** (needed for dynamic IM/BIM pod respawns). Failure mode is locked to **fail-hard** for now — single-path per peer, no redundancy, revisit only if Phase 5 reveals a real problem.

Sub-phases land in order; each is independently committed and reconciled.

#### Phase 4a — whereabouts IPAM install

- [x] Scaffold a new Flux app at `kubernetes/apps/network/whereabouts/` using the same `GitRepository` + Flux inner `Kustomization` + `spec.patches` pattern pioneered for Multus in Phase 1 (no Helm chart ships a thick-mode-equivalent for whereabouts v0.x either). Source: `github.com/k8snetworkplumbingwg/whereabouts`, pinned tag via Renovate `github-releases`. Render `doc/crds/whereabouts.cni.cncf.io_ippools.yaml` + `doc/crds/whereabouts.cni.cncf.io_overlappingrangeipreservations.yaml` + `doc/crds/daemonset-install.yaml`, patching the DaemonSet + ServiceAccount + CRB into the `network` namespace (same namespace move pattern as Multus).
- [x] Verify post-reconcile: `kubectl get crd | rg whereabouts` shows both CRDs; `kubectl -n network get ds whereabouts` reports 3/3 Running; no Cilium or Multus regression.

#### Phase 4b — per-node VTEP loopbacks + static routes (Talos rolling apply)

- [x] Edit `talos/talconfig.yaml`: add one `/32` VTEP address per node as a per-node `patches:` `DummyLinkConfig` document (talhelper v3.1.7 silently drops `networkInterfaces[].dummy: true`; see 2026-04-19 Log), and add two `routes:` entries on each node's existing SFP+ `/31` interfaces pointing at peer VTEPs. Addressing:
  - k8s-1 VTEP `10.100.100.1/32` on dummy0; routes `10.100.100.2/32 via 10.100.0.1` (DAC #1) and `10.100.100.3/32 via 10.100.0.3` (DAC #2).
  - k8s-2 VTEP `10.100.100.2/32`; routes `10.100.100.1/32 via 10.100.0.0` and `10.100.100.3/32 via 10.100.0.5`.
  - k8s-3 VTEP `10.100.100.3/32`; routes `10.100.100.1/32 via 10.100.0.2` and `10.100.100.2/32 via 10.100.0.4`.
- [x] `task talos:generate-config`, review the rendered diff on each node (expect one `DummyLinkConfig` doc per node + two `LinkConfig` entries per SFP+ `/31` carrying `routes:`).
- [x] Hand off to `talos-operator` for rolling apply (same pattern as Phase 2). Adding a dummy + routes is additive and should resolve to `MODE=auto` → no-reboot on all three nodes; if it doesn't, STOP and surface.
- [x] Verify on each node: `talosctl -n <ip> get addresses` shows the `/32` VTEP; `talosctl -n <ip> get routes` shows the two peer-VTEP routes; ping each peer's VTEP from the local VTEP (3 × 2 = 6 pings) via host-network netshoot pods, 0 % loss.

#### Phase 4c — VXLAN overlay DaemonSet

Talos `talconfig.yaml` cannot natively declare a VXLAN interface (no `kind: vxlan` in `machine.network.interfaces` on Talos v1.12). The overlay is provisioned by a small Flux-managed DaemonSet under `kubernetes/apps/network/storage-vxlan/`.

- [x] Scaffold a new Flux app at `kubernetes/apps/network/storage-vxlan/` (3-file pattern + raw resources — no chart, just a DaemonSet + RBAC + ConfigMap). DaemonSet spec:
  - `hostNetwork: true`, `hostPID: false`, tolerates control-plane taint, pinned one-per-node via the default DaemonSet behavior.
  - Single container, image `nicolaka/netshoot` (or a smaller purpose-built image — TBD during authoring), `securityContext.capabilities.add: [NET_ADMIN]`, no other privileges.
  - Command reads the node name via downward-API env `NODE_NAME`, looks up the per-node VTEP IP and the two peer VTEP IPs in a ConfigMap mounted as env vars (so the same image runs on every node), then executes idempotently:
    ```
    ip link show vxlan-storage || ip link add vxlan-storage type vxlan id 100 \
        local $NODE_VTEP dstport 4789 nolearning
    ip link set vxlan-storage mtu 8950 up
    ip addr replace $NODE_OVERLAY_IP/24 dev vxlan-storage
    bridge fdb replace 00:00:00:00:00:00 dev vxlan-storage dst $PEER1_VTEP self
    bridge fdb append 00:00:00:00:00:00 dev vxlan-storage dst $PEER2_VTEP self
    ```
  - After setup, the container sleeps indefinitely (tail -f /dev/null style) so that `kubectl` / Flux see it as Ready; on restart it re-applies (the `||` + `replace` make it idempotent).
  - Overlay-side addressing: k8s-1 `10.100.1.1/24`, k8s-2 `10.100.1.2/24`, k8s-3 `10.100.1.3/24` on `vxlan-storage`.
  - MTU accounting: underlay 9000 − (VXLAN 8 + UDP 8 + IPv4 20 + Ethernet 14) = 8950 overlay MTU. Set on both the vxlan interface and in the NAD's `mtu` field.
- [x] Verify post-reconcile: `kubectl -n network get ds storage-vxlan` 3/3 Running; on each node, `nsenter` (or another netshoot pod) confirms `vxlan-storage` interface exists with the expected IP and MTU. Ping `10.100.1.2` and `10.100.1.3` from `10.100.1.1` (and bidirectional from the other two nodes) via host-network pods — expect 0 % loss across the overlay.

#### Phase 4d — NetworkAttachmentDefinition + install-cni init container + smoke test

- [x] Author `kubernetes/apps/storage/longhorn-config/app/networkattachment.yaml` — `NetworkAttachmentDefinition` named `longhorn-storage` in the **`storage`** namespace. Spec:
  ```
  type: macvlan, mode: bridge, master: vxlan-storage, mtu: 8950
  ipam: whereabouts, range: 10.100.1.0/24, exclude: 10.100.1.0/28
  ```
  The `/28` exclusion reserves `.1-.3` (the node-side VTEP overlay IPs) plus a small buffer; pods will draw from `.16-.254`. Landed as a new sibling Flux app `kubernetes/apps/storage/longhorn-config/` (3-file pattern) rather than inside the `longhorn` HelmRelease app — chart-vs-config separation survives Longhorn chart churn and gives future RecurringJobs / BackingImages / BackupTarget a natural home. Committed + reconciled in `a5ff463f`; `kubectl -n storage get net-attach-def longhorn-storage` Ready.
- [x] **Add a `ghcr.io/siderolabs/install-cni:v1.7.0` init container to the Multus DaemonSet** — append one JSON Patch entry to `kubernetes/apps/network/multus/app/flux-kustomization.yaml`'s `spec.patches` list, targeting `kube-multus-ds`. Reuses the upstream DS's existing `cnibin` hostPath volume (verified against `k8snetworkplumbingwg/multus-cni@v4.2.4`) with `mountPropagation: Bidirectional`. Per ADR 0018 and the authoritative Sidero guidance at <https://docs.siderolabs.com/kubernetes-guides/cni/multus>. Replaces the original Option A (Talos `siderolabs/cni-plugins` system extension) which proved non-viable — see 2026-04-19 Log entries below. No new Flux app, no Talos-layer change, no reboot. Verify post-reconcile: `kubectl -n network logs ds/kube-multus-ds -c install-cni --tail=40` shows `macvlan` being copied; `talosctl -n <ip> list /opt/cni/bin` on each of k8s-1/2/3 now contains `macvlan`.
- [x] Smoke test: run a netshoot pod with annotation `k8s.v1.cni.cncf.io/networks: longhorn-storage` pinned to k8s-1, and another on k8s-2 with the same annotation. Confirm each pod gets a `10.100.1.x` address via whereabouts on `net1`, and that pod-to-pod ping across the overlay succeeds. Tear the test pods down.

### Phase 5 — Longhorn storageNetwork + verify

**Blocked 2026-04-19 on first attempt — macvlan host↔local-pod isolation breaks Longhorn v1 engine iSCSI frontend.** The flip was applied and reverted the same session; the existing kps volumes recovered cleanly on the default network. See the Log entry of 2026-04-19 for the full diagnosis; short version: Longhorn's v1 engine runs its iSCSI *target* inside the IM pod (at the `lhnet1` address) but its iSCSI *initiator* (`iscsiadm` / kernel) runs in the host netns, and macvlan-in-bridge-mode blocks host→same-host-pod traffic by design. Pod↔pod across nodes works (Phase 4d smoke test), so the NAD itself is fine for that path — but Longhorn needs the host→IM direction too, which macvlan does not provide.

Phase 5a added: swap the NAD's `type: macvlan` for `type: ipvlan` in L2 mode at `kubernetes/apps/storage/longhorn-config/app/networkattachment.yaml`. ipvlan L2 shares the parent interface's MAC, so host↔pod on the same subnet works unchanged. Same whereabouts IPAM, same overlay, same `10.100.1.0/24`. One known ipvlan caveat that does not apply here: ipvlan parent must not be in bridge mode — our parent is `vxlan-storage` which is a plain L2 vxlan interface, so we're clean.

- [x] **Phase 5a — swap macvlan → ipvlan L2 in the NAD.** *Tried and disproven the same session — see 2026-04-19 Log.* ipvlan L2 has the same structural host-isolation against a vxlan parent that macvlan-bridge does (different mechanism, same outcome: tcpdump showed the host's ARP encapsulated by `vxlan-storage` and sent to remote VTEPs instead of looping into the local ipvlan child).
- [x] **Phase 5b — macvlan host-shim child of `vxlan-storage`.** Replaced ipvlan L2. Extends `storage-vxlan` DaemonSet to create `lhnet1-host` as a peer macvlan child and migrate the per-node 10.100.1.X address off the parent and onto the shim. macvlan-bridge sidesteps parent-child isolation by allowing child-to-child forwarding without traversing the parent's egress. NAD reverted to macvlan/bridge to match the shim's plugin family.
- [x] Edit the Longhorn `HelmRelease` values at `kubernetes/apps/storage/longhorn/app/helmrelease.yaml` to set `defaultSettings.storageNetwork: storage/longhorn-storage` (namespace corrected from the original `longhorn-system/…`).
- [x] Drain replica traffic safely: scale the workload causing writes down first (if any), or let Longhorn rebuild replicas under the new network post-setting.
- [x] Commit, `task reconcile`, wait for Longhorn pods to cycle. Verify `kubectl -n storage get pods -o yaml | rg 'k8s.v1.cni.cncf.io/networks'` shows IM / BIM / replica pods annotated with `storage/longhorn-storage`, and each has a `10.100.1.x` address in its pod status. Also verify volumes reach `attached/healthy` under the new network (the macvlan attempt flapped here — this is the blocking-gate verification).
- [x] Create a test PVC + pod that writes ~10 GiB. While the write runs, query `sum by (instance) (rate(node_network_transmit_bytes_total{device=~"enp2s0f.np."}[30s]))` in Prometheus — expect it to light up. Query the same rate for `device="enp87s0"` — expect it to stay near baseline for replica bytes.
- [x] Record the observed replica throughput in the Log; compare to the Phase 3 iperf3 number. Expect meaningful headroom shrinkage from the 9.76–9.90 Gbit/s raw baseline due to Longhorn protocol overhead *plus* VXLAN encap (~50 B/packet, minor for large replica blocks); should still be well above the 2.5 GbE management-network ceiling.
- [x] Tear down the test workload.

## Log

- 2026-04-18: Opened from ADR 0017 (Multus adoption) which is itself a follow-up to ADR 0009 (SFP+ mesh). Prerequisites landed earlier today: mesh cabled and link-up on all 6 ports, per-port MAC mapping discovered via ARP-probe + node-exporter RX counter deltas (recorded in `context/hardware.md`), symmetric 2+2+2 Longhorn topology achieved via k8s-2 `longhorn-2` userVolume (plan 0001 follow-up completed).
- 2026-04-18: Phase 1 install mechanism pivoted from `HelmRelease` + OCI HelmRepository to `GitRepository` + Flux `Kustomization` + kustomize overlay. Driver: no usable Helm chart exists for Multus v4 thick-plugin mode (evidence in next entry).
- 2026-04-18: Chart landscape as researched today. (a) Official `k8snetworkplumbingwg/helm-charts/multus` is Chart v0.1.2 / appVersion 0.1.0, image `v3.8` thin-mode only, last meaningful commit 2022; the chart template has no thick-plugin knobs and hardcodes `kube-system`. (b) Bitnami `multus-cni` chart v2.2.22 / appVersion 4.2.2 is OCI-distributed at `oci://registry-1.docker.io/bitnamicharts/multus-cni` but depends on `docker.io/bitnami/multus-cni` which Broadcom paywalled on 2025-08-28 (images moved to `docker.io/bitnamilegacy`, frozen with no CVE fixes unless $50K–$72K/yr BSI subscription); the chart also ships a single-DaemonSet wrapper, not the upstream daemon+shim thick-mode shape. (c) Upstream's supported thick-mode install is `deployments/multus-daemonset-thick.yml` on `k8snetworkplumbingwg/multus-cni`, tagged per release (v4.2.4, Feb 2025). Option chosen: `GitRepository` at that repo + kustomize overlay. Fits anton's 3-file pattern structurally (source + ks + `app/`), keeps Renovate on the git tag.
- 2026-04-18: ADR 0017's body still reads "installed via its official Helm chart" and a follow-up bullet says "OCI HelmRepository + thick-mode values". Those sentences pre-date this research and are factually incorrect. The ADR's **decision** (adopt Multus thick-plugin chained to Cilium, Longhorn sole consumer, under `kubernetes/apps/network/multus/`) is unchanged, so ADR 0017 is not being superseded. This Log entry is the durable correction pointer per the ADR-vs-plan split: ADRs record *why*, plans record *how / what's next*.
- 2026-04-18: Phase 1 scaffolded via `flux-app-author`. Files: `kubernetes/apps/network/multus/{ks.yaml,app/kustomization.yaml,app/gitrepository.yaml,app/flux-kustomization.yaml}`. `GitRepository` pinned to `v4.2.4` with Renovate `github-releases` datasource; image pinned to `ghcr.io/k8snetworkplumbingwg/multus-cni:v4.2.4-thick` (HTTP 200 verified). Inner Flux `Kustomization` `multus-upstream` renders `deployments/multus-daemonset-thick.yml` with four namespace-move patches (DS `kube-multus-ds`, SA `multus`, CM `multus-daemon-config`, CRB `multus` subject[0]). Dual-Kustomization shape is intentional — Flux `Kustomization.spec.patches` is the only way to patch external `GitRepository` content; plain kustomize cannot consume a Flux source. Upstream thick-mode `multus-daemon-config` uses `"multusConfigFile": "auto"` (verified against the v4.2.4 YAML), so the originally planned `clusterNetwork: cilium` patch was dropped as a no-op; auto-discovery picks up Cilium's CNI config at `/host/etc/cni/net.d`. Phase 1 task 2 wording updated to match.
- 2026-04-18: **Blocked.** Running `task configure` after scaffolding surfaced ~34 files of template drift — the Multus registration added to `kubernetes/apps/network/kustomization.yaml` was silently stripped by the template render, and unrelated hand-edits (Cilium `devices: enp+`, resource limits, Hubble relay config, envoy-gateway, cloudflare-tunnel, spegel, talconfig truncation, a re-materialized `echo-two/`) reverted to their template-rendered shape. Root cause: anton was bootstrapped from the onedr0p cluster-template but `task template:tidy` was never run, so `templates/` remains authoritative and any direct edit under `kubernetes/` that isn't mirrored back into `templates/` is overwritten on the next `task configure`. Scaffolding further Flux apps through the current toolchain (which mandates `task configure` per root CLAUDE.md) will keep clobbering hand-edits. **Unblock condition:** (1) run `task template:tidy` to archive the template machinery to `.private/`; (2) update root CLAUDE.md and `.taskfiles/CLAUDE.md` to drop the "always run `task configure` before commit" rule in favour of direct `kubernetes/` edits; (3) re-scaffold Phase 1 Multus files fresh. Until then, pausing all Phase 1–5 execution. Multus design decisions above remain valid and do not need re-deciding post-tidy.
- 2026-04-19: **Unblocked → In-progress.** `task template:tidy` landed, root / .taskfiles / agents / skills / hooks were scrubbed of stale `task configure` and `task template:reset` references, and SOPS round-trip was re-verified. Phase 1 re-scaffolded fresh via `flux-app-author` and committed as `c4d4c937` (`feat(network): scaffold Multus v4.2.4 thick-plugin`). Files: `kubernetes/apps/network/multus/{ks.yaml,app/kustomization.yaml,app/gitrepository.yaml,app/flux-kustomization.yaml}` + registration line in `kubernetes/apps/network/kustomization.yaml`. Post-push `task reconcile` applied revision `c4d4c937`; `flux get ks -n network` reports `multus` Ready=True and `multus-upstream` Ready=True at `v4.2.4@sha1:705a59ea`; `kubectl -n network get ds kube-multus-ds` shows 3/3 Running on k8s-1/2/3; `cilium status` green, 64/64 pods managed by Cilium — no CNI regression. Phase 1 acceptance criterion met. Next: Phase 2 Talos `/31` rolling config.
- 2026-04-19: **Phase 2 complete.** SFP+ `/31` addressing committed as `da63955e` (`feat(talos): add SFP+ /31 networkInterfaces`) — six new `networkInterfaces` entries across the three nodes (k8s-1 a0/a1, k8s-2 b9/b8, k8s-3 a9/a8), mgmt interfaces untouched. `task talos:generate-config` rendered the expected `ethSel1`/`ethSel2` `LinkConfig` blocks per node with `mtu: 9000`. Rolling apply via `talos-operator` — `MODE=auto` resolved to **no-reboot** on all three nodes (purely additive link config, kernel hot-add). Rolled k8s-1 → k8s-2 → k8s-3 with etcd quorum re-verified between each (3/3 healthy throughout). All 6 `/31` addresses present in `talosctl get addresses`; all 6 SFP+ ports `LINK STATE=true`; mgmt `192.168.1.x/24` + VIP `192.168.1.101` on k8s-3 intact. All 6 bidirectional pings succeeded via host-network netshoot pods (talosctl has no remote ping subcommand) — RTT 0.26–0.36 ms across every DAC pair, 0 % loss. Cilium 3/3, nodes Ready, no membership change. Phase 2 acceptance criterion met. Next: Phase 3 `iperf3` baseline (≥9 Gbit/s gate).
- 2026-04-19: **Phase 3 complete.** iperf3 baseline ran against three ephemeral `hostNetwork: true` pods (one per node, `nicolaka/netshoot` image with `iperf3 -s`, pinned via `nodeSelector`). Pre-flight jumbo-frame check (`ping -M do -s 8972`) passed 0 % loss across all six directions, confirming MTU 9000 is end-to-end clean (no path-MTU black hole, no silent fragmentation). Full `iperf3 -c <peer-/31> -B <local-/31> -P 4 -t 30 -f g` results: **DAC #1 k8s-1→k8s-2 9.90 Gbit/s / k8s-2→k8s-1 9.81 Gbit/s; DAC #2 k8s-1→k8s-3 9.90 / k8s-3→k8s-1 9.76; DAC #3 k8s-2→k8s-3 9.90 / k8s-3→k8s-2 9.90.** All six directions clear the ≥9 Gbit/s gate — fabric fully matches ADR 0009's capacity promise. Pattern noted (not blocking): traffic *into* k8s-1 shows elevated TCP retries (DAC #1 rev 346, DAC #2 rev 588) vs the 70–100 range everywhere else; probably IRQ-affinity / RSS-queue asymmetry on the i9-13900H P/E-core split, revisit only if Phase 5 replica bytes expose it as a real ceiling. Pods torn down, manifest at `/tmp/iperf3-phase3.yaml` removed. Phase 3 acceptance criterion met. Next: Phase 4 `NetworkAttachmentDefinition` (IPAM mode TBD — whereabouts preferred for replica-count scalability).
- 2026-04-19: Status confirmed `In-progress` (no-op flip — plan was already In-progress after Phase 1 unblock). Fixed a lingering `longhorn-system` → `storage` reference in the References section. Active sub-phase is **4a — whereabouts install**.
- 2026-04-19: **Phase 4 scope expanded; direction locked.** Interview with operator surfaced two gaps the original Phase 4 glossed over. (a) *Namespace error* — the plan text and Phase 5 all referred to `longhorn-system`, but Longhorn is actually Helm-released into `storage` (verified via `kubectl -n storage get hr longhorn`, chart v1.11.1). Top-level acceptance criterion + Phase 4 + Phase 5 text corrected to use `storage/longhorn-storage`. (b) *Topology-adapter gap* — the three point-to-point `/31` DAC links from Phase 2 are three separate L2 domains, so a single macvlan-on-SFP+ NAD cannot span them (a pod attached via macvlan-on-enp2s0f0np0 on k8s-1 can only reach k8s-2, not k8s-3). No central switch exists (full-mesh DAC cabling). Decision: layer a **VXLAN overlay (VNI 100)** on top of the three `/31`s to present a shared `10.100.1.0/24` segment; NAD is then plain macvlan on `vxlan-storage` with whereabouts IPAM — the pattern Longhorn's docs assume. Alternatives considered and rejected: ipvlan L3 + per-node sub-pools (non-standard, fragments the single-NAD model, requires nodeSliceSize whereabouts config); buying a 10G switch and redoing Phase 2 with a shared `/24` star (invalidates Phases 2+3 work, multi-week hardware lead). **Failure mode deferred** — pick fail-hard (Longhorn rebuilds on DAC failure, same as node failure), revisit after Phase 5 proves the baseline. Phase 4 re-decomposed into 4a (whereabouts install via `GitRepository` + spec.patches pattern), 4b (Talos /32 loopback VTEPs + peer routes via rolling apply), 4c (VXLAN overlay DaemonSet under `kubernetes/apps/network/storage-vxlan/`, since Talos `machine.network.interfaces` doesn't expose a `vxlan` kind), 4d (NAD + smoke test). Talos v1.12 pre-check: whereabouts CRDs not yet installed (confirmed via `kubectl get crd`).
- 2026-04-19: **Phase 4a complete.** Whereabouts v0.9.3 scaffolded via `flux-app-author` at `kubernetes/apps/network/whereabouts/` using the same `GitRepository` + `spec.patches` pattern as Multus, committed as `fe870d3c` (`feat(network): scaffold whereabouts v0.9.3 (plan 0004 phase 4a)`) + chore `43240c82`. Three deltas vs the original Phase 4a brief, all handled inside the scaffold: (a) upstream `doc/crds/` ships 5 files, not 3 — narrowed via GitRepository `ignore:` to the DS + two CRDs; (b) upstream DaemonSet image is a floating `:latest` — pinned with an extra `spec.patches` entry to `ghcr.io/k8snetworkplumbingwg/whereabouts:v0.9.3` and a second Renovate annotation (`datasource=docker`); (c) a `whereabouts-config` ConfigMap ships with the DS and needed a namespace-move patch, in addition to the DS/SA/CRB moves. Post-reconcile: `flux get ks -n network` reports `whereabouts` + `whereabouts-upstream` both Ready=True at `v0.9.3@sha1:c4bb29f7`; both CRDs present at cluster scope; `kubectl -n network get ds whereabouts` 3/3 Running (pods scheduled on all three nodes, image tag matches the pin); Cilium 64/64 managed, Multus 3/3 Running — no CNI regression. Phase 4a acceptance criteria met. Next: Phase 4b Talos `/32` VTEP loopbacks + peer routes (additive, should resolve to `MODE=auto` no-reboot).
- 2026-04-19: **Phase 4b complete.** VTEP loopbacks + SFP+ peer routes committed as `26db7f58` (`feat(talos): add VTEP loopbacks + SFP+ peer routes (plan 0004 phase 4b)`). **Gotcha discovered and worked around:** talhelper v3.1.7 silently drops the `networkInterfaces[].dummy: true` bool — confirmed via `pkg/talos/networkconfig.go` (`GenerateLinkConfig` unconditionally emits `LinkConfigV1Alpha1`, never `DummyLinkConfigV1Alpha1`). Talos v1.12 requires a separate multi-doc `apiVersion: v1alpha1 / kind: DummyLinkConfig` document with the dummy's name + addresses; `LinkConfig` and `DummyLinkConfig` are mutually exclusive (`conflictingLinkKinds`). Solution landed inline: remove the dummy from `networkInterfaces[]` entirely and inject a `DummyLinkConfig` as a per-node `patches:` item ahead of the existing kubelet-mounts patch. Rendered correctly afterward — each node config now has two `LinkConfig` entries (SFP+ /31s with embedded `routes:`) + one `DummyLinkConfig` entry (dummy0 with /32 VTEP). `talosctl validate --mode=metal` clean on all three. Rolling apply via `talos-operator` — **MODE=auto resolved to no-reboot on all three nodes** (apply wall times 277–300 ms each), etcd stayed 3/3 healthy with raft term 28 stable and leader unchanged, VIP stayed on k8s-3, management interfaces + all six pre-existing /31s intact. Six-direction VTEP ping matrix via host-network netshoot pods: 0 % loss everywhere, RTT 0.07–0.49 ms — indistinguishable from Phase 2's bare-/31 numbers, confirming VTEP traffic rides the correct DAC via the per-`/31` peer-VTEP route. One non-blocking operational note: parallel `kubectl exec` through the tailscale-operator apiserver proxy recycled sockets and produced transient `use of closed network connection` errors; sequential execs worked first try. Phase 4b acceptance criteria met. Next: Phase 4c VXLAN overlay DaemonSet at `kubernetes/apps/network/storage-vxlan/`.
- 2026-04-19: **Phase 4c complete.** `storage-vxlan` DaemonSet scaffolded via `flux-app-author` at `kubernetes/apps/network/storage-vxlan/` — 3-file Flux pattern, raw manifests only (no HelmRelease, no GitRepository source): `ks.yaml` + `app/{kustomization,serviceaccount,configmap,daemonset}.yaml`. Image `nicolaka/netshoot:v0.13` pinned with Renovate `datasource=docker`, `hostNetwork: true`, `capabilities: { add: [NET_ADMIN], drop: [ALL] }`, per-node VTEP/overlay mapping via `storage-vxlan-nodemap` ConfigMap sourced at `/etc/vxlan-nodemap/${NODE_NAME}.env`. Feat committed as `4c4feb22`. **Two discovery-at-runtime fixes required** before the DS stabilised, both committed separately to keep blame narrative clear. (a) Flux postBuild envsubst stripped every unescaped `${VAR}` reference in the DS `args:` to the empty string (none are defined in `cluster-secrets`), which yielded an empty `NODEMAP` path and immediate FATAL. Fix in `737a80ce` — escape every bash var reference as `$${VAR}` so Flux emits a literal `$`; keeps the postBuild boilerplate consistent with sibling network apps. (b) Talos v1.12.6 kernel (6.18.18) rejects `bridge fdb replace` on non-unicast MAC entries with `Cannot replace non-unicast fdb entries` → the DS surfaced this as `RTNETLINK answers: Not supported`. Initial hypothesis (vxlan kernel module missing) was wrong — `talos-operator` added `machine.kernel.modules: [{name: vxlan}]` via a global patch at `talos/patches/global/machine-kernel.yaml` (committed as `7f1433e9`, rolling no-reboot apply on all three nodes), then confirmed **vxlan is compiled built-in on this kernel** — `/lib/modules/.../modules.builtin` lists `vxlan.ko` and `/sys/module/vxlan` exists on all three nodes, so `/proc/modules | grep vxlan` is permanently empty and is the wrong presence test for this Talos version. The module declaration is kept as declarative hygiene (no-op against builtins) but does not fix the real bug. The real fix landed in `d8759fab` — swap the `replace`-then-`append` fdb pattern for `del ... || true` then `append` on both peers, making the script idempotent without relying on a `replace` verb the kernel refuses. Post-reconcile verification: `flux get ks -n network storage-vxlan` Ready=True at `d8759fab`; `kubectl -n network get ds storage-vxlan` 3/3 Running on k8s-1/2/3; each pod logged `[storage-vxlan] up on k8s-N (10.100.1.N)`; `ip -br addr show vxlan-storage` per node shows the expected `/24` and MTU 8950; `bridge fdb show dev vxlan-storage` per node shows exactly two peer VTEP entries with no duplicates. Six-direction overlay ping via host-network netshoot pods with `NET_RAW` (the DS itself only has `NET_ADMIN`, no raw-socket need): **0 % loss, RTT 0.28–0.43 ms across every pair** — on par with the Phase 2 `/31` and Phase 4b VTEP baselines, confirming the VXLAN encap cost is negligible for small packets. Two `talos-operator` agent-memory entries accrued: `reference_raw_daemonset_pattern.md` (local-manifest 3-file variant) and `project_vxlan_kernel_module.md` (builtin-vs-loadable presence test). Phase 4c acceptance criteria met. Next: Phase 4d `NetworkAttachmentDefinition` in the `storage` namespace + smoke test.
- 2026-04-19: **Phase 4d partial — NAD landed, blocker discovered, scope expanded.** Scaffolded the NAD as a new sibling Flux app `kubernetes/apps/storage/longhorn-config/` (3-file pattern: `ks.yaml` + `app/{kustomization,networkattachment}.yaml`) rather than inside the `longhorn` HelmRelease app. Chart-vs-config separation was the homelab-fit choice: keeps Longhorn chart upgrades atomic, matches the `cert-manager` vs `cert-manager-issuers` shape used elsewhere, and gives future `RecurringJob` / `BackingImage` / `BackupTarget` + ExternalSecret a natural home without a later refactor. Registered in `storage/kustomization.yaml`, committed as `a5ff463f` (`feat(storage): longhorn-storage NAD (plan 0004 phase 4d)`), reconciled clean — `flux get ks -n storage longhorn-config` Ready=True, `kubectl -n storage get net-attach-def longhorn-storage` present with the expected macvlan-on-`vxlan-storage` + whereabouts 10.100.1.0/24 exclude 10.100.1.0/28 config. **Smoke test surfaced a blocker the plan had not anticipated:** two netshoot pods with `k8s.v1.cni.cncf.io/networks: longhorn-storage` failed to start on k8s-1 and k8s-2 with `plugin type="multus-shim" … error adding container to network "longhorn-storage": failed to find plugin "macvlan" in path [/opt/cni/bin]`. `talosctl -n 192.168.1.98 list /opt/cni/bin` confirms Talos v1.12.6 ships only `bridge / cilium-cni / firewall / flannel / host-local / loopback / passthru / portmap` by default; `multus-shim` and `whereabouts` are already present because their Phase 1 / Phase 4a DaemonSets install-drop them from the container image — neither path covers `macvlan`. Decision locked: **Option A, install the Talos `siderolabs/cni-plugins` system extension** rather than spinning up an in-cluster DaemonSet to drop a macvlan binary into `/opt/cni/bin`. Reasons: (a) declarative via the Talos image factory schematic, Renovate-trackable, atomic with Talos upgrades; (b) ships *all* upstream reference plugins at once (macvlan, ipvlan, vlan, tuning…) so any future NAD needing a different plugin is covered with no second intervention; (c) matches the same chart-vs-config / declarative-over-imperative principle used five hours earlier for `longhorn-config` placement. Cost accepted: one rolling `MODE=auto` reboot per node (schematic swap is a new machine image). Smoke-test pods torn down via `--grace-period=0 --force`; the committed NAD is harmless until a workload references it, so no revert needed. Hand-off next: `talos-operator` for the schematic swap + rolling apply, then the smoke test repeats.
- 2026-04-19: **Option A dead — pivot to Option C (install-cni init container) per official Sidero guidance. ADR 0018 captures the new decision.** `talos-operator` ran the schematic swap hand-off and discovered `siderolabs/cni-plugins` is not in the Talos v1.12.6 factory extension catalog. The factory POST accepted the new schematic (`6687487c…`) because it does not validate extension names at create time, but every subsequent installer pull returned HTTP 400 `error enhancing profile from schematic: official extension "siderolabs/cni-plugins" is not available for Talos version v1.12.6`. Cross-verified against `factory.talos.dev/version/v1.12.6/extensions/official` (80+ entries, zero `cni*` match) and `github.com/siderolabs/extensions/tree/release-1.12/network/` (no `cni-plugins` directory) — the extension existed in older Talos releases and was removed from the catalog before 1.12, because siderolabs pivoted to letting CNI consumers drop their own reference plugins via DaemonSets. Recovery was surgical: `talos-operator` re-`apply-config --mode=no-reboot`'d k8s-1 with the original `445a99db…` installer URL to scrub the failed `6687487c…` reference that the earlier `apply-node MODE=auto` had persisted into machine config (important subtlety: "applied without a reboot" is NOT a no-op for install image changes — the reference is stored and would be used on the next reboot trigger). All three nodes' active machineconfig now references `445a99db…`; `jj status` on `talconfig.yaml` and `nodes.yaml` is clean; etcd stayed 3/3 throughout; no node ever rebooted; pre-attempt etcd snapshot kept at `~/anton-backup-20260419/etcd-pre-cni-plugins.db` for insurance. The operator accrued agent-memory entries `project_cni_plugins_extension_removed.md` and an `apply-node MODE=auto` subtlety note. **Pivot to Option C**: fetch the official Sidero guidance at <https://docs.siderolabs.com/kubernetes-guides/cni/multus> which prescribes adding a `ghcr.io/siderolabs/install-cni:v1.7.0` init container to the Multus DaemonSet, mounting `/host/opt/cni/bin` with `mountPropagation: Bidirectional` and running `/install-cni.sh` to drop macvlan + friends. Verified upstream `k8snetworkplumbingwg/multus-cni@v4.2.4/deployments/multus-daemonset-thick.yml` already ships the `cnibin` hostPath volume (it's how `multus-shim` itself lands on nodes), so the patch is additive — one new JSON Patch entry on the existing inner Flux Kustomization at `kubernetes/apps/network/multus/app/flux-kustomization.yaml`, no new Flux app, no Talos-layer change, no reboot. The `install-cni` image is labeled "deprecated" by Sidero only in the narrow sense that Talos v1.8+ bundles the essential plugin subset (bridge/firewall/host-local/loopback/portmap); for users needing macvlan (which we do), the image remains the documented path with no alternative being promoted. Decision recorded as ADR 0018 (`context/adrs/0018-install-cni-init-container-for-multus-reference-plugins.md`); Phase 4d bullet two above updated to reflect Option C. Next: land the flux-kustomization.yaml patch, reconcile, verify macvlan on `/opt/cni/bin` on all three nodes, re-run the smoke test.
- 2026-04-19: **Phase 4d complete.** Option C landed in three atomic commits — `84d0e878` (`feat(network): install-cni init container for Multus`), `fa02bba0` (`docs(context): ADR 0018 + plan 0004 Option C pivot`), `2ef28ad1` (`chore(agent-memory): talos-operator — cni-plugins extension not in v1.12.6 catalog`) — pushed as main, reconciled clean. The Multus DS rolled in ~20s (3/3 Running, no crash, init-container statuses both `exitCode: 0`); install-cni is silent-by-design (no stdout, but post-flight proves the copy worked). `talosctl -n <ip> list /opt/cni/bin` on all three nodes now shows the full reference set: **macvlan, ipvlan, vlan, tuning, ptp, dhcp, sbr, vrf, bandwidth, host-device, dummy, static, tap** — plus the pre-existing bundled set (bridge/firewall/host-local/loopback/portmap/passthru/flannel/cilium-cni) and DS-dropped shims (multus-shim, whereabouts). Smoke test passed on the first retry: two netshoot pods (host-network off, Multus annotation `k8s.v1.cni.cncf.io/networks: longhorn-storage`) both `Ready` within seconds; k8s-1 pod drew `10.100.1.16/24` on `net1`, k8s-2 pod drew `10.100.1.17/24` — exactly the whereabouts `.16-.254` band (the `/28` exclusion correctly reserved `.0-.15` including the VTEP `.1/.2/.3`). Bidirectional ping across the overlay: **0 % loss, RTT 0.53–0.84 ms**, on par with but slightly above the Phase 4c host-level VXLAN baseline of 0.28–0.43 ms — the delta is the extra macvlan + pod-netns hop, not a problem. Test pods torn down via `--grace-period=0 --force`. Full acceptance criteria met:
  - [x] Multus thick-plugin mode deployed (Phase 1)
  - [x] SFP+ /31 mesh configured + 6/6 link state (Phase 2)
  - [x] iperf3 ≥9 Gbit/s all six directions (Phase 3)
  - [x] `NetworkAttachmentDefinition longhorn-storage` exists in `storage` namespace and is usable (Phase 4d)
  - [ ] Longhorn `storageNetwork` flip + replica-byte verification — **Phase 5, next session**

  Open loop remaining for Phase 5: set `defaultSettings.storageNetwork: storage/longhorn-storage` in `kubernetes/apps/storage/longhorn/app/helmrelease.yaml`, let Longhorn rebuild replicas, verify `node_network_transmit_bytes_total` rate lights up on `enp2s0f*` devices (SFP+) during volume writes while `enp87s0` (2.5 GbE mgmt) stays flat. Expected replica throughput well above the 2.5 GbE management-path ceiling and moderately below the 9.76–9.90 Gbit/s raw iperf3 baseline (Longhorn protocol + VXLAN encap overhead).
- 2026-04-19: **Phase 5 first attempt — flipped and reverted; root cause identified; Phase 5a added.** Committed `ebe62a5c` (`feat(storage): route Longhorn replicas onto SFP+ mesh`) at 17:58 UTC, Flux reconciled clean, Longhorn `storage-network` CRD took the value `storage/longhorn-storage`. Only the k8s-2 IM auto-cycled (it had no attached engines); k8s-1 and k8s-3 IMs stayed on the default network because they were serving the three attached kps volumes (Prometheus 50Gi, Grafana 5Gi, Alertmanager 2Gi — all on k8s-1 at flip time). To force the flip, suspended `kube-prometheus-stack` HR, scaled Prometheus/Alertmanager CRDs and Grafana Deployment to 0 → all three volumes detached → all three IMs restarted with `lhnet1` in their `k8s.v1.cni.cncf.io/network-status` annotation (10.100.1.16 on k8s-2, 10.100.1.17 on k8s-3, 10.100.1.18 on k8s-1). Scaled kps back up → engines tried to reattach → volumes went into an attach/detach flap loop, robustness=unknown, repeated `DetachedUnexpectedly` events. **Root cause in the longhorn-manager logs:** `iscsiadm: cannot make connection to 10.100.1.16: Host is unreachable`. The engine's replica traffic was fine — longhorn-manager logged `Updating engine current replica address map to map[…-r-…:10.100.1.17:10110 …-r-…:10.100.1.18:10090]` and the replicas logged `New connection from: 10.100.1.16:<port>` — proving pod↔pod over the SFP+ overlay works. The break is elsewhere: Longhorn's **v1 data engine publishes an iSCSI target inside the IM pod (on `lhnet1`) but its iSCSI initiator runs in the host netns** (kernel `iscsiadm` via nsenter into the host mount/net namespace). Macvlan-in-bridge-mode — which we used for the NAD — intentionally isolates host↔same-host-pod traffic: a macvlan child gets its own MAC and the host's parent interface does not receive frames for it, even when both sit in the same /24. The cross-host path works (remote hosts' MACs are reachable via the VXLAN fabric), but every engine-to-target attach has to talk to the *local* IM pod first, and macvlan blocks that. The Phase 4d pod-pair smoke test validated pod↔pod (which macvlan does support) but did not exercise host↔pod, which is why the gap slipped through. Reverted to restore observability: removed the `storageNetwork` line from the HR (commit `09621f8b`), reconciled, manually cleared the Longhorn setting CRD (`kubectl patch settings.longhorn.io storage-network --type=merge -p '{"value":""}'` — Helm chart removal does not propagate into the setting CRD, which is a Longhorn-side persistence choice), re-scaled kps down so IMs could cycle WITHOUT the lhnet1 annotation (setting CRD needs the IMs to re-read it; IMs only re-read on restart; IMs only restart when idle), scaled kps back up. Final state: all three kps volumes `attached/healthy` on k8s-2 via default cilium network, Prometheus pod Ready, kps HR Ready at 83.5.0, no data loss. Plan revision: added **Phase 5a** — swap NAD `type: macvlan` → `type: ipvlan, mode: l2` at `kubernetes/apps/storage/longhorn-config/app/networkattachment.yaml`. ipvlan shares the parent MAC, so host↔local-pod in the same /24 works. No other moving parts change: same whereabouts pool, same `vxlan-storage` parent, same `10.100.1.0/24`, same /28 exclusion. The Phase 4d smoke test extends to add a host→pod reachability check on each node before retrying the storageNetwork flip. Alternative rejected: a "macvlan + host-side shim macvlan on the parent" workaround — more moving parts, requires another per-node script, and ipvlan L2 is the idiomatic fix for this exact case per both Multus docs and Longhorn operator guidance. One non-blocking pattern from this session: when troubleshooting a flapping Longhorn volume, the answer lives in the **engine** instance log (via `printInstanceLogs` forwarded by longhorn-manager), not the replica log — the replica only sees clean connects and EOFs; the engine is the one whose iSCSI syscall fails. Log this as a future triage signal.
- 2026-04-19: **Phase 5a tried and disproven; Phase 5b host-shim landed; Phase 5 closes Done.** Phase 5a swapped the NAD to ipvlan L2 (commit `2a4cd478`) on the hypothesis that ipvlan does not enforce the host-isolation that broke the macvlan attempt. The hypothesis was wrong. Live tcpdump on k8s-1 with the Whereabouts-assigned probe pod proved the actual failure mode: the host's ARP for the local pod's `lhnet1` IP gets sent OUT the `vxlan-storage` parent (encapsulated to remote VTEPs) and never reaches the local ipvlan child. Different mechanism, same gate failure as macvlan. Pod↔pod cross-node was clean (the parent's RX path delivers vxlan-decapped frames to ipvlan children fine), so the smoke test that only exercised pod↔pod would have falsely passed — the host→local-pod check is what catches it. Web research confirmed the architectural answer: the Longhorn storage-network doc only requires *pod-to-pod* reachability and silently assumes a flat L2 underlay (`longhorn/longhorn#9049` is open since 2024-07 with the same failure on macvlan + an L2 underlay; no upstream fix). Our `/31` mesh + VXLAN overlay violates that assumption — the parent is a vxlan device whose egress always encapsulates, so neither macvlan-bridge nor ipvlan-L2 can give the host a local-pod path. Pivoted to **Phase 5b — macvlan host-shim child of `vxlan-storage`** (commit `f327139c`). Extended the `storage-vxlan` DaemonSet to (a) create `lhnet1-host` as a peer macvlan-bridge child of `vxlan-storage`, MTU 8950, and (b) migrate the per-node 10.100.1.X address off the parent and onto the shim. Mechanism: macvlan-bridge isolates parent↔child but allows child↔child by hashing dest MACs and short-circuiting intra-host delivery without entering the parent's `ndo_start_xmit` (which is what triggers vxlan encapsulation). With the shim, the host iSCSI initiator is itself a macvlan child and can reach co-located IM pods directly; remote-host traffic still falls through to the parent and rides the vxlan tunnel as before. NAD reverted to `macvlan/bridge` to match the shim's plugin family. Smoke test (full reachability matrix on a live cluster, before the HR flip): pod↔pod cross-node ~0.5 ms (vxlan), **host→local-pod ~0.1 ms** (intra-host child↔child, the gate), host→remote-pod ~0.5 ms (shim → vxlan) — all 0% loss. Re-flipped the Longhorn HR (commit `fd073943`) and forced IMs to cycle by scaling kps StatefulSets + Grafana to 0; all three IMs came back with `lhnet1` in 10.100.1.16-18 and the three kps volumes attached/healthy on k8s-2 with `STORAGEIP=10.100.1.17` on both engine and local replica — same-node iSCSI through the shim, exactly the path macvlan + ipvlan both broke. Throughput test (`/tmp/phase5-write-test.yaml`, 10 GiB urandom dd, 2-replica volume on k8s-2 + k8s-3): dd was urandom-bounded at **146 MiB/s** (~1.2 Gbps payload, CPU not network); per-interface rate over a 2-min window covering the burst showed k8s-2 SFP+ ports at **790 Mbps TX each** (both DAC peers active), `vxlan-storage` at 1562 Mbps TX (sum of both peer legs at the encap layer), `lhnet1-host` at 797 Mbps TX (host shim handing traffic to the local IM); k8s-3 SFP+ at **988 Mbps RX** matched by `vxlan-storage` at 978 Mbps RX (decap). The 2.5 GbE mgmt interface (`enp87s0`) saw **0.4–0.5 Mbps** of Longhorn traffic across both nodes — within noise. Replica replication and same-node iSCSI both ride the SFP+ mesh; mgmt path is unloaded. Goal of plan 0004 met. One non-blocking pattern from this session: scaling Flux-managed StatefulSets via `kubectl scale` is fragile because Flux re-pins them on the next reconcile; for forced IM cycling in the future, suspend the relevant `HelmRelease` first or scale via the Helm value. The address migration in the storage-vxlan DaemonSet (`ip addr del … vxlan-storage || true; ip addr replace … lhnet1-host`) is idempotent across both legacy state (addr on parent) and steady-state restarts. Plan closes Done.

## References

- Related ADRs: 0009 (SFP+ full-mesh fabric), 0017 (Multus adoption)
- Related plans: 0001 (Longhorn adoption — Phase 3 fio baseline established the 2.5 GbE ceiling this plan lifts)
- Hardware inventory: `context/hardware.md` — per-DAC MAC + port mapping and the ARP-probe discovery method
- Talos config: `talos/talconfig.yaml` — where the Phase 2 `networkInterfaces` edits land
- Cluster checks: `kubectl -n storage get net-attach-def`, `talosctl get links`, `talosctl get addresses`
- Multus upstream: https://github.com/k8snetworkplumbingwg/multus-cni
