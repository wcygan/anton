---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: longhorn-config
spec:
  interval: 1h
  path: ./kubernetes/apps/storage/longhorn-config/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: storage
  wait: false
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
