---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dragonfly-operator
spec:
  chart:
    spec:
      chart: ./charts/dragonfly-operator
      sourceRef:
        kind: GitRepository
        name: dragonfly-operator
  interval: 1h
  values: {}
