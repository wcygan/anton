---
apiVersion: cluster.redpanda.com/v1alpha2
kind: Redpanda
metadata:
  name: redpanda-playground
spec:
  chartRef: {}
  clusterSpec:
    # Minimal single-node configuration
    statefulset:
      replicas: 1

    # Minimal resource requests (Redpanda needs at least 610Mi)
    resources:
      cpu:
        cores: 500m
      memory:
        container:
          min: 1Gi
          max: 2Gi

    # Small storage
    storage:
      persistentVolume:
        enabled: true
        size: 2Gi

    # Disable external access for testing
    external:
      enabled: false
