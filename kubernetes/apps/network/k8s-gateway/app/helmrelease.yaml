---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: k8s-gateway
spec:
  chartRef:
    kind: OCIRepository
    name: k8s-gateway
  interval: 1h
  values:
    fullnameOverride: k8s-gateway
    domain: "${SECRET_DOMAIN}"
    ttl: 1
    # Internal DNS resolver for HTTPRoute hosts on 192.168.1.102 — losing
    # this strands every internal request that resolves through it.
    priorityClassName: system-cluster-critical
    service:
      type: LoadBalancer
      port: 53
      annotations:
        lbipam.cilium.io/ips: "192.168.1.102"
      externalTrafficPolicy: Cluster
    watchedResources: ["HTTPRoute", "Service"]
