---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: homepage
spec:
  imageRepositoryRef:
    name: homepage
  filterTags:
    pattern: '^main-\d+-[a-f0-9]+$'
  policy:
    alphabetical:
      order: asc
