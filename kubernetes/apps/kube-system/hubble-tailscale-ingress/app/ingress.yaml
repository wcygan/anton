---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hubble
  namespace: kube-system
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - hubble
  defaultBackend:
    service:
      name: hubble-ui
      port:
        number: 80
