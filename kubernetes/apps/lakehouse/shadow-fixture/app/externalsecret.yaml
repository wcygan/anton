---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shadow-fixture-s3
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: shadow-fixture-s3
  dataFrom:
    - extract:
        key: seaweedfs-iceberg-shadow
